const N_GRID_SIZE: number = 15;

let grid: number[][] = Array(N_GRID_SIZE).fill(Array(N_GRID_SIZE).fill(1));

export default function SpiralMovement() {
  return (
    <></>
  );
}

function Cell() {
  return <div className="border m-auto w-4 h-4">5</div>;
}
